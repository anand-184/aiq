name: aiq
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.6.0
  cloud_firestore: ^6.2.0
  google_fonts: ^8.0.2
  shared_preferences: ^2.3.0
  firebase_auth: ^6.3.0
  provider: ^6.1.1
  shimmer: ^3.0.0
  http: ^1.1.0 # Added for EmailJS (Free OTP sending)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
